[project]
name = "scanner-map-backend"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["fastapi", "uvicorn", "sqlalchemy", "pydantic-settings"]

[tool.pytest.ini_options]
testpaths = ["tests"]
